/**
 * Warning options that are used by Fader and other widgets that show warnings.
 * These options control warning behavior when values exceed their range.
 */
export interface IWarningOptions {
  /** If true, shows a warning when the value exceeds the range. */
  show_warning?: boolean;
}
